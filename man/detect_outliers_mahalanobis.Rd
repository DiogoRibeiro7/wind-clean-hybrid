% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outlier_mahalanobis.R
\name{detect_outliers_mahalanobis}
\alias{detect_outliers_mahalanobis}
\title{Detect Outliers via Mahalanobis Distance}
\usage{
detect_outliers_mahalanobis(data, alpha = 0.95, reg_eps = 1e-06)
}
\arguments{
\item{data}{data.frame with columns wind_speed, power, and cluster.}

\item{alpha}{confidence level for chi-squared threshold (default 0.95).}

\item{reg_eps}{regularization epsilon added to diagonal when covariance is
singular (default 1e-6). Set to 0 to disable regularization.}
}
\value{
tibble with added `outlier` column. The returned object carries a
  `"singular_clusters"` attribute — a named list mapping cluster ids to the
  reason (e.g. `"regularized"`, `"diagonal_fallback"`, `"skipped"`) when the
  covariance could not be used as-is.
}
\description{
Detect Outliers via Mahalanobis Distance
}
